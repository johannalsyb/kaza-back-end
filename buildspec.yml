version: 0.2
phases:
  pre_build:
    commands:
      - echo "Pre-build phase started"
      - aws --version
  build:
    commands:
      - echo "Build phase started - Kaza Backend"
      - echo "Docker deployment - no build required"
  post_build:
    commands:
      - echo "Build completed for Kaza"
artifacts:
  files:
    - '**/*'
